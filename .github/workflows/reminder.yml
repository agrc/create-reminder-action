name: 'reminder'
on:
  issue_comment:
    types: [created, edited]
jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
    - uses: agrc/reminder-action@dev
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
